import type { CompilerInput } from "hardhat/types";
import type { ICompiler, ResolcConfig } from "../types";
export declare function compile(resolcConfig: ResolcConfig, input: CompilerInput): Promise<import("@parity/resolc").SolcOutput>;
export declare class BinaryCompiler implements ICompiler {
    config: ResolcConfig;
    constructor(config: ResolcConfig);
    compile(input: CompilerInput): Promise<import("@parity/resolc").SolcOutput>;
}
export declare class NpmCompiler implements ICompiler {
    config: ResolcConfig;
    constructor(config: ResolcConfig);
    compile(input: CompilerInput): Promise<import("@parity/resolc").SolcOutput>;
}
