import { HardhatNetworkUserConfig } from "hardhat/types/config";
import { RpcServer } from "../types";
export declare function createRpcServer(opts: {
    docker?: HardhatNetworkUserConfig["docker"];
    nodePath?: string;
    adapterPath?: string;
    isForking?: boolean;
}): RpcServer;
