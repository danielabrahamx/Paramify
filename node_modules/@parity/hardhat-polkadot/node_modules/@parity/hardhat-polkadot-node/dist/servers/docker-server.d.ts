import { HardhatNetworkUserConfig } from "hardhat/types/config";
import { RpcServer } from "../types";
export declare class DockerRpcServer implements RpcServer {
    private adapterContainer;
    private nodeContainer;
    private docker;
    constructor(dockerSocketPath?: Extract<HardhatNetworkUserConfig["docker"], string>);
    private pruneContainerByName;
    listen(nodeArgs?: string[], adapterArgs?: string[], blockProcess?: boolean): Promise<void>;
    stop(): Promise<void>;
}
